import { createStore } from "vuex";

export default createStore({
  // state: {

  // },
  state() {
    return {
      noteData: { a: "first note vuex" },
    };
  },
  mutations: {},
  actions: {},
  modules: {},
});
